<Window x:Class="bismuthIDE.src.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        mc:Ignorable="d"
        Title="Bismuth IDE" Height="450" Width="800">
    <Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/> <ColumnDefinition Width="5"/>   <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

    <Menu Grid.Row="0" Grid.ColumnSpan="3">
        <MenuItem Header="File">
            <MenuItem Header="OpenFile" Click="OpenFileButton_Click"/>
            <MenuItem Header="OpenFolder" Click="OpenFolderButton_Click"/>
        </MenuItem>
        <MenuItem Header="Edit">
            <MenuItem Header="Undo"/>
            <MenuItem Header="Fix"/>
        </MenuItem>
        <MenuItem Header="Run">
            <MenuItem Header="Run" Click="RunButton_Click"/>
        </MenuItem>
    </Menu>

    <TreeView Grid.Row="1" Grid.Column="0" Name="FileExplorer"
              SelectedItemChanged="FileExplorer_SelectedItemChanged">
    </TreeView>

    <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#ccc"/>

    <Grid Grid.Row="1" Grid.Column="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>    <RowDefinition Height="5"/>    <RowDefinition Height="150"/>  </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Name="EditorTabs">
        </TabControl>

        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="#ccc"/>

        <TextBox Grid.Row="2"
                 Name="OutputLog"
                 IsReadOnly="True"
                 Background="#1e1e1e"
                 Foreground="White"
                 FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Text="--- Console ---&#x0a;"/>
    </Grid>
</Grid>
</Window>